apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: observability
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: http
      name: http