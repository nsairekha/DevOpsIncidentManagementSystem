/**
 * Backwards compatibility re-export of demoData.ts
 * (kept so older imports from "./demo-data" keep working).
 * Canonical isolated development fallback lives in ./demoData.
 */
export * from "./demoData";
