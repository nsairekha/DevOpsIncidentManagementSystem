# Prometheus scrape configuration for the AI-Based Cloud Observability System.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ai-cloud-observability-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]